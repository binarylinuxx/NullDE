# zwm - Minimal C++ Wayland Window Manager Base

## Requirements
- CMake
- g++ or clang++
- wayland-client development files (`libwayland-dev`)

## Build
```sh
mkdir build && cd build
cmake ..
make
```

## Run
```sh
./zwm
```

## Config
Edit `config.ini` to change settings. 